from setuptools import setup, find_packages

setup(
    name='inpsimulator',
    version='0.1.0',
    include_package_data=True,
    install_requires=[
    ],
    python_requires=">=3.7",
    packages=find_packages(),
)